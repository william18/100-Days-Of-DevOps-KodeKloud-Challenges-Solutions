# 1. Go to the repository
cd /usr/src/kodekloudrepos/games

# 2. Make sure you are on master branch
git checkout master

# 3. Pull the latest changes (optional but good practice)
git pull origin master

# 4. Create a new branch named xfusioncorp_games from master
git checkout -b xfusioncorp_games

# 5. Push the new branch to remote (if required by Nautilus setup)
git push origin xfusioncorp_games
